'use client';

import {useAccount} from "wagmi";
import {FetchedWalletToken} from "@v3/@special/wallet/hooks";

export async function doVerification(account: ReturnType<typeof useAccount>, token: FetchedWalletToken) {

}
